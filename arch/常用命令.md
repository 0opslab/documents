### hadoop常用命令

```bash
# 上传文件
hadoop fs -put <localsrc> ... <dst>

# put file and run mapreduce on yarn
hadoop fs -put /root/anaconda-ks.cfg /
bin/yarn jar share/hadoop/mapreduce/hadoop-mapreduce-examples-2.7.1.jar wordcount /anaconda-ks.cfg /out
```

