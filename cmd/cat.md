# title{cat - 命令最常用的命令之一}

```bash
# Display the contents of a file
#显示文件的内容
cat /path/to/foo

# Display contents with line numbers
#显示带行号的内容
cat -n /path/to/foo

# Display contents with line numbers (blank lines excluded)
#显示包含行号的内容（不包括空白行）
cat -b /path/to/foo
```